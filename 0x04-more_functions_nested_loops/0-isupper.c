#include "main.h"
/**
 * _isupper - checks for uppercase character
 * @b: character to be checked
 * Return: 1 if b is uppercase and 0 if otherwise
 */
int _isupper(int b)
{
if (b >= 65 && b <= 90)
{
return (1);
}
return (0);
}
