#include "main.h"
/**
 * add - adds two integers and returns the result
 * @n1: 1st character to be checked
 * @n2: 2nd character to be checked
 * Return: adds two integers and returns the result
 */
int add(int n1, int n2)
{
return (n1 + n2);
}
