#include <stdio.h>
/**
 * main - program that prints the lowercase alphabet in reverse
 * Return: 0 (BIG WIN)
 */
int main(void)
{
int c;
c = 'z';
while
(c >= 'a')
{
putchar(c);
c--;
}
putchar('\n');
return (0);
}
