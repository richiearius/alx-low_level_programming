#include <stdio.h>
/**
 * main - program that prints the alphabet in lowercase then  a new line.
 * Return: 0 (BIG WIN)
 */
int main(void)
{
int p;
p = 'a';
while
(p <= 'z')
{
putchar (p);
p++;
}
putchar('\n');
return (0);
}
