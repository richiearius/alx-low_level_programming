#include <stdio.h>
/**
 * main - a program that prints the alphabet in lowercase, followed by a new line.
 * Return: 0 (BIG WIN)
 */
int main(void)
{
	int p;

	p = 'a';
	while
		(p <= 'z') {
			putchar (p);
			p++;
		}
	putchar('\n');
	return (0);
}
