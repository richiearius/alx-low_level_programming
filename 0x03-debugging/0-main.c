#include "main.h"
/**
 * main - test for a postitive_or_negative() function
 * @i: character to be checked
 * Return: 0 (BIG WIN)
 */
int main(void)
{
int i;
i = 0;
positive_or_negative(i);
return (0);
}
